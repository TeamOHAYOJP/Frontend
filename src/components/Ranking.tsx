import React, { VFC } from 'react';

interface PROPS {
  name: string;
  age: number;
  id: number;
}

const Ranking = (props: PROPS) => {
  return (
    <div className="">
    </div>
  )
}

export default Ranking;
